services:
  react-app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - /app/node_modules
      - .:/app

  test:
    build: .

    volumes:
      - /app/node_modules
      - .:/app
    command: ["npm", "run", "test"]
